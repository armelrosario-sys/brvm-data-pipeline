streamlit>=1.40,<2
pandas>=2.0
altair>=5.0
PyYAML>=6.0

# Verrou de compatibilite (05/08/2026) : starlette 1.4.0 a rendu obligatoire le
# parametre thread_minimum_size de GZipResponder.__init__(). Streamlit 1.61.0
# appelle encore ce constructeur sans ce parametre, ce qui fait repondre le
# serveur 500 a tous les health checks au demarrage (l'application ne demarre
# jamais). Le defaut est chez Streamlit, pas dans app.py. A retirer des qu'une
# version de Streamlit corrigeant l'appel sera publiee.
starlette<1.4
